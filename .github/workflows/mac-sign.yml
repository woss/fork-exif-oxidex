name: Mac Sign

on:
  push:
    branches: ["main"]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'
      - '.gitignore'

jobs:
  build-and-release:
    runs-on: warp-macos-15-arm64-6x
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Extract version from Cargo.toml
        id: version
        run: |
          VERSION=$(grep '^version' Cargo.toml | head -1 | cut -d'"' -f2)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Install dependencies
        run: |
          brew install just

      - name: Import signing certificate
        env:
          BUILD_CERTIFICATE_BASE64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          # Create variables
          CERTIFICATE_PATH=$RUNNER_TEMP/build_certificate.p12
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db

          # Decode certificate from base64
          echo -n "$BUILD_CERTIFICATE_BASE64" | base64 --decode -o $CERTIFICATE_PATH

          # Create temporary keychain
          security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH

          # Import certificate to keychain
          security import $CERTIFICATE_PATH -P "$P12_PASSWORD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
          security list-keychain -d user -s $KEYCHAIN_PATH

          # Allow codesign to access keychain
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH

          # Debug: List available signing identities
          echo "Available signing identities:"
          security find-identity -v -p codesigning $KEYCHAIN_PATH

      - name: Build release
        env:
          DEVELOPMENT_TEAM: ${{ secrets.DEVELOPMENT_TEAM }}
        run: |
          just build-release

      - name: Sign binary
        env:
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          APP_PATH="./target/release/oxidex"
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db

          # Unlock keychain for codesign access
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH

          # Automatically detect the Developer ID Application certificate
          IDENTITY=$(security find-identity -v -p codesigning $KEYCHAIN_PATH | grep "Developer ID Application" | head -1 | grep -o '".*"' | tr -d '"')

          if [ -z "$IDENTITY" ]; then
            echo "Error: No Developer ID Application certificate found"
            exit 1
          fi

          echo "Found signing identity: $IDENTITY"
          echo "Signing binary with Developer ID certificate..."
          codesign --sign "$IDENTITY" \
            --timestamp \
            --options runtime \
            --verbose \
            --keychain $KEYCHAIN_PATH \
            "$APP_PATH"

          echo "Verifying signature..."
          codesign --verify --verbose "$APP_PATH"

          echo "Displaying signature details..."
          codesign --display --verbose=4 "$APP_PATH"

          echo "✓ Binary signed successfully"

      - name: Create DMG
        run: |
          brew install create-dmg
          just create-dmg v${{ steps.version.outputs.version }}

      - name: Notarize DMG
        env:
          NOTARIZATION_APPLE_ID: ${{ secrets.NOTARIZATION_APPLE_ID }}
          NOTARIZATION_PASSWORD: ${{ secrets.NOTARIZATION_PASSWORD }}
          NOTARIZATION_TEAM_ID: ${{ secrets.NOTARIZATION_TEAM_ID }}
        run: |
          DMG_PATH="dist/oxidex-v${{ steps.version.outputs.version }}.dmg"

          echo "Submitting DMG for notarization..."
          SUBMISSION_OUTPUT=$(xcrun notarytool submit "$DMG_PATH" \
            --apple-id "$NOTARIZATION_APPLE_ID" \
            --password "$NOTARIZATION_PASSWORD" \
            --team-id "$NOTARIZATION_TEAM_ID" \
            --wait 2>&1)

          echo "$SUBMISSION_OUTPUT"

          # Extract submission ID (first occurrence only, trim whitespace)
          SUBMISSION_ID=$(echo "$SUBMISSION_OUTPUT" | grep -E "^\s+id:" | head -1 | awk '{print $2}' | tr -d '[:space:]')

          echo "Extracted submission ID: $SUBMISSION_ID"

          # Check if submission was accepted
          if echo "$SUBMISSION_OUTPUT" | grep -q "status: Accepted"; then
            echo "✓ Notarization accepted"
          else
            echo "❌ Notarization failed, fetching log..."
            xcrun notarytool log "$SUBMISSION_ID" developer_log.json \
              --apple-id "$NOTARIZATION_APPLE_ID" \
              --password "$NOTARIZATION_PASSWORD" \
              --team-id "$NOTARIZATION_TEAM_ID"

            echo "Notarization error details:"
            cat developer_log.json
            exit 1
          fi

          echo "Stapling notarization ticket to DMG..."
          xcrun stapler staple "$DMG_PATH"

          echo "Verifying DMG notarization..."
          xcrun stapler validate "$DMG_PATH"

          echo "✓ DMG notarized and stapled successfully"

      - name: Clean up keychain
        if: always()
        run: |
          security delete-keychain $RUNNER_TEMP/app-signing.keychain-db || true

    #   - name: Create GitHub Release
    #     uses: softprops/action-gh-release@v2
    #     with:
    #       name: Release ${{ steps.version.outputs.tag }}
    #       draft: false
    #       prerelease: false
    #       files: |
    #         dist/oxidex-v${{ steps.version.outputs.version }}.dmg
    #       body: |
    #         ## Oxidex v${{ steps.version.outputs.version }}

    #         ### Installation

    #         1. Download the DMG file below
    #         2. Open the DMG
    #         3. Drag MediaControl.app to the Applications folder
    #         4. Double-click to launch
    #         5. Grant Accessibility permissions for F10/F11/F12 media keys

    #         ### Features

    #         - **F10**: Mute toggle
    #         - **F11**: Volume down
    #         - **F12**: Volume up
    #         - Menu bar controls for volume and mute
    #         - Persistent receiver IP configuration
    #         - Version info display

    #         ### Requirements

    #         - macOS 13.0 or later
    #         - Onkyo/Integra receiver with eISCP network support

    #         ### Note

    #         This app is **signed and notarized** by Apple.
    #         - Developer: SWACKTECH, LLC
    #         - No security warnings on installation
    #         - Safe to run directly
    #     env:
    #       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
