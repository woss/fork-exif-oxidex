name: Test

on:
  push:
    branches: ["main"]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'
      - '.gitignore'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  CARGO_INCREMENTAL: 1
  CARGO_PROFILE_DEV_DEBUG: 0
  CARGO_PROFILE_TEST_DEBUG: 0

jobs:
  test:
    name: Test Suite
    runs-on: warp-ubuntu-latest-x64-2x
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4
        with:
          lfs: true

      - name: Cache Rust toolchain
        uses: WarpBuilds/cache@v1
        with:
          path: |
            ~/.rustup/toolchains
            ~/.rustup/update-hashes
            ~/.rustup/settings.toml
          key: ${{ runner.os }}-${{ runner.arch }}-rustup-stable-${{ hashFiles('**/rust-toolchain.toml') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-rustup-stable-
            ${{ runner.os }}-${{ runner.arch }}-rustup-

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@5d458579430fc14a04a08a1e7d3694f545e91ce6 # stable

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@89e9040de88b577a072e3760aaf59f585da083af # v0.0.5

      - name: Setup Rust cache
        uses: WarpBuilds/rust-cache@v2
        with:
          cache-on-failure: true
          cache-provider: warpbuild

      - name: Build
        run: cargo build --release --verbose --all-features

      - name: Run tests
        run: cargo test --verbose --all-features

  clippy:
    name: Clippy Lints
    runs-on: warp-ubuntu-latest-x64-2x
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4
        with:
          lfs: false

      - name: Cache Rust toolchain
        uses: WarpBuilds/cache@v1
        with:
          path: |
            ~/.rustup/toolchains
            ~/.rustup/update-hashes
            ~/.rustup/settings.toml
          key: ${{ runner.os }}-${{ runner.arch }}-rustup-stable-${{ hashFiles('**/rust-toolchain.toml') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-rustup-stable-
            ${{ runner.os }}-${{ runner.arch }}-rustup-

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@5d458579430fc14a04a08a1e7d3694f545e91ce6 # stable

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@89e9040de88b577a072e3760aaf59f585da083af # v0.0.5

      - name: Setup Rust cache
        uses: WarpBuilds/rust-cache@v2
        with:
          cache-provider: warpbuild

      - name: Run Clippy
        run: cargo clippy --all-features -- -D warnings

  fmt:
    name: Code Formatting
    runs-on: warp-ubuntu-latest-x64-2x
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4
        with:
          lfs: false

      - name: Cache Rust toolchain
        uses: WarpBuilds/cache@v1
        with:
          path: |
            ~/.rustup/toolchains
            ~/.rustup/update-hashes
            ~/.rustup/settings.toml
          key: ${{ runner.os }}-${{ runner.arch }}-rustup-stable-rustfmt-${{ hashFiles('**/rust-toolchain.toml') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-rustup-stable-rustfmt-
            ${{ runner.os }}-${{ runner.arch }}-rustup-stable-
            ${{ runner.os }}-${{ runner.arch }}-rustup-

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@5d458579430fc14a04a08a1e7d3694f545e91ce6 # stable
        with:
          components: rustfmt

      - name: Check formatting
        run: cargo fmt --all -- --check

  integration-tests:
    name: Integration Tests (ExifTool Comparison)
    runs-on: warp-ubuntu-latest-x64-2x
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4
        with:
          lfs: true

      - name: Cache Rust toolchain
        uses: WarpBuilds/cache@v1
        with:
          path: |
            ~/.rustup/toolchains
            ~/.rustup/update-hashes
            ~/.rustup/settings.toml
          key: ${{ runner.os }}-${{ runner.arch }}-rustup-stable-${{ hashFiles('**/rust-toolchain.toml') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-rustup-stable-
            ${{ runner.os }}-${{ runner.arch }}-rustup-

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@5d458579430fc14a04a08a1e7d3694f545e91ce6 # stable

      - name: Setup Rust cache
        uses: WarpBuilds/rust-cache@v2
        with:
          cache-on-failure: true
          cache-provider: warpbuild

      - name: Install Perl ExifTool
        run: |
          if ! command -v exiftool &> /dev/null; then
            echo "ExifTool not found, installing..."
            sudo apt-get update -qq
            sudo apt-get install -y libimage-exiftool-perl
          else
            echo "ExifTool already installed: $(exiftool -ver)"
          fi

      - name: Build ExifTool-RS
        run: cargo build --release --all-features

      - name: Run integration tests with ExifTool comparison
        run: cargo test --release --features exiftool-comparison -- --nocapture
